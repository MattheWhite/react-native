# Initialize expo RN project

>> npx create-expo-app --template blank   OR: bare-minimum/tabs

>> npm install

>> npm run android

NPX  -> command-line tools for executing node_modules withoiut global installation
